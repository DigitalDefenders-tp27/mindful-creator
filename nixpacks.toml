[phases.setup]
nixPkgs = ["python3", "python3Packages.pip", "python3Packages.setuptools", "python3Packages.wheel", "postgresql", "git", "git-lfs"]

[phases.install]
cmds = [
  "python -m venv .venv",
  "source .venv/bin/activate && cd backend && pip install -r requirements.txt"
]

[start]
cmd = "source .venv/bin/activate && cd backend && sh railway_startup.sh" 